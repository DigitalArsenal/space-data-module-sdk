# SDK Skills

Use the minimum skill needed for the task.

- `wasmedge-pthreads`
  - Path: `.claude/skills/wasmedge-pthreads/Skills.md`
  - Use when building or reviewing WasmEdge-targeted module artifacts, thread
    model selection, or system-Emscripten pthread compilation.
- `runtime-wrapper-audit`
  - Path: `.claude/skills/runtime-wrapper-audit/Skills.md`
  - Use when deciding whether a runtime can host the guest contract directly or
    needs a wrapper, and when reviewing claims about WasmEdge compatibility.
