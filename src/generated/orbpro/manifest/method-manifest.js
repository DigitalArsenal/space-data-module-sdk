// automatically generated by the FlatBuffers compiler, do not modify
/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */
import * as flatbuffers from "flatbuffers";
import { DrainPolicy } from "../../orbpro/manifest/drain-policy.js";
import { PortManifest, } from "../../orbpro/manifest/port-manifest.js";
/**
 * Canonical method declaration.
 */
export class MethodManifest {
    bb = null;
    bb_pos = 0;
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsMethodManifest(bb, obj) {
        return (obj || new MethodManifest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsMethodManifest(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new MethodManifest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    methodId(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset
            ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
            : null;
    }
    displayName(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset
            ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
            : null;
    }
    inputPorts(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset
            ? (obj || new PortManifest()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb)
            : null;
    }
    inputPortsLength() {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    outputPorts(index, obj) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset
            ? (obj || new PortManifest()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb)
            : null;
    }
    outputPortsLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    maxBatch() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint32(this.bb_pos + offset) : 1;
    }
    drainPolicy() {
        const offset = this.bb.__offset(this.bb_pos, 14);
        return offset
            ? this.bb.readUint8(this.bb_pos + offset)
            : DrainPolicy.DRAIN_UNTIL_YIELD;
    }
    description(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 16);
        return offset
            ? this.bb.__string(this.bb_pos + offset, optionalEncoding)
            : null;
    }
    static startMethodManifest(builder) {
        builder.startObject(7);
    }
    static addMethodId(builder, methodIdOffset) {
        builder.addFieldOffset(0, methodIdOffset, 0);
    }
    static addDisplayName(builder, displayNameOffset) {
        builder.addFieldOffset(1, displayNameOffset, 0);
    }
    static addInputPorts(builder, inputPortsOffset) {
        builder.addFieldOffset(2, inputPortsOffset, 0);
    }
    static createInputPortsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startInputPortsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addOutputPorts(builder, outputPortsOffset) {
        builder.addFieldOffset(3, outputPortsOffset, 0);
    }
    static createOutputPortsVector(builder, data) {
        builder.startVector(4, data.length, 4);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addOffset(data[i]);
        }
        return builder.endVector();
    }
    static startOutputPortsVector(builder, numElems) {
        builder.startVector(4, numElems, 4);
    }
    static addMaxBatch(builder, maxBatch) {
        builder.addFieldInt32(4, maxBatch, 1);
    }
    static addDrainPolicy(builder, drainPolicy) {
        builder.addFieldInt8(5, drainPolicy, DrainPolicy.DRAIN_UNTIL_YIELD);
    }
    static addDescription(builder, descriptionOffset) {
        builder.addFieldOffset(6, descriptionOffset, 0);
    }
    static endMethodManifest(builder) {
        const offset = builder.endObject();
        builder.requiredField(offset, 4); // method_id
        return offset;
    }
    static createMethodManifest(builder, methodIdOffset, displayNameOffset, inputPortsOffset, outputPortsOffset, maxBatch, drainPolicy, descriptionOffset) {
        MethodManifest.startMethodManifest(builder);
        MethodManifest.addMethodId(builder, methodIdOffset);
        MethodManifest.addDisplayName(builder, displayNameOffset);
        MethodManifest.addInputPorts(builder, inputPortsOffset);
        MethodManifest.addOutputPorts(builder, outputPortsOffset);
        MethodManifest.addMaxBatch(builder, maxBatch);
        MethodManifest.addDrainPolicy(builder, drainPolicy);
        MethodManifest.addDescription(builder, descriptionOffset);
        return MethodManifest.endMethodManifest(builder);
    }
    unpack() {
        return new MethodManifestT(this.methodId(), this.displayName(), this.bb.createObjList(this.inputPorts.bind(this), this.inputPortsLength()), this.bb.createObjList(this.outputPorts.bind(this), this.outputPortsLength()), this.maxBatch(), this.drainPolicy(), this.description());
    }
    unpackTo(_o) {
        _o.methodId = this.methodId();
        _o.displayName = this.displayName();
        _o.inputPorts = this.bb.createObjList(this.inputPorts.bind(this), this.inputPortsLength());
        _o.outputPorts = this.bb.createObjList(this.outputPorts.bind(this), this.outputPortsLength());
        _o.maxBatch = this.maxBatch();
        _o.drainPolicy = this.drainPolicy();
        _o.description = this.description();
    }
}
export class MethodManifestT {
    methodId;
    displayName;
    inputPorts;
    outputPorts;
    maxBatch;
    drainPolicy;
    description;
    constructor(methodId = null, displayName = null, inputPorts = [], outputPorts = [], maxBatch = 1, drainPolicy = DrainPolicy.DRAIN_UNTIL_YIELD, description = null) {
        this.methodId = methodId;
        this.displayName = displayName;
        this.inputPorts = inputPorts;
        this.outputPorts = outputPorts;
        this.maxBatch = maxBatch;
        this.drainPolicy = drainPolicy;
        this.description = description;
    }
    pack(builder) {
        const methodId = this.methodId !== null ? builder.createString(this.methodId) : 0;
        const displayName = this.displayName !== null ? builder.createString(this.displayName) : 0;
        const inputPorts = MethodManifest.createInputPortsVector(builder, builder.createObjectOffsetList(this.inputPorts));
        const outputPorts = MethodManifest.createOutputPortsVector(builder, builder.createObjectOffsetList(this.outputPorts));
        const description = this.description !== null ? builder.createString(this.description) : 0;
        return MethodManifest.createMethodManifest(builder, methodId, displayName, inputPorts, outputPorts, this.maxBatch, this.drainPolicy, description);
    }
}
//# sourceMappingURL=method-manifest.js.map